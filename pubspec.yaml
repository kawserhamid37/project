name: project
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.4
  intl: ^0.17.0


  cupertino_icons: ^0.1.2
  flutter_svg: ^0.19.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  provider: ^6.0.1


flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
